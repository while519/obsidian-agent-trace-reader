{
  "name": "agent-trace-reader",
  "version": "0.2.0",
  "private": true,
  "description": "Obsidian Agent Trace Reader plugin",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test test/parse.test.js",
    "check": "npm run build && node --check main.js && npm test"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
